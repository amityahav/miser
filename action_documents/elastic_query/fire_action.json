{
  "rule_id": "{{rule.id}}",
  "rule_name": "{{rule.name}}",
  "rule_type": "{{rule.type}}",
  "alert": {
    "alert_id": "{{alert.id}}",
    "status": "active",
    "triggered": "{{context.date}}",
    "value": "{{context.value}}",
    "context_message": "{{context.message}}",
    "custom_data": {"key": "val"}
  }
}